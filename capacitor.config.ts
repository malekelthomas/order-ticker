import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
   appId: 'io.ionic.starter',
   appName: 'order-ticker',
   webDir: 'build',
   bundledWebRuntime: false,
}

export default config
